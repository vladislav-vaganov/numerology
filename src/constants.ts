const now = new Date();
export const MIN_BIRTH_DATE = new Date('01.01.1000');
export const MAX_BIRTH_DATE = now;
export const CURRENT_YEAR = now.getFullYear();

export const COLORS = {
  background: '#ffffff',
  primary: '#00838f',
  secondary: '#f50057',
};
